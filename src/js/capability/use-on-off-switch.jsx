import useProperty from "../../hooks/useProperty";
import {useState} from "react";


export default function useOnOffSwitch(prop){
   const onOffProperty = useProperty(prop)


   function findProperties(){
      if(prop.description.properties){

      }
   }
}